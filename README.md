This is a simple authentication in MERN Stack.

It saves users, keeps track of their sessions and allows you to login or logout from different devices as the same user.

Used JWT, bcrypt, ua-parser, cookie-parser for authentication mechanism.